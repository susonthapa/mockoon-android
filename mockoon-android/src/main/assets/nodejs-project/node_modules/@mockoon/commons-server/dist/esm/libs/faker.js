import faker from 'faker';
/**
 * Set the Faker locale
 *
 * @param locale
 */
export const SetFakerLocale = (locale) => {
    faker.locale = locale;
};
/**
 * Set the Faker seed
 *
 * @param seed
 */
export const SetFakerSeed = (seed) => {
    if (seed !== undefined && seed !== null) {
        faker.seed(seed);
    }
};
//# sourceMappingURL=faker.js.map