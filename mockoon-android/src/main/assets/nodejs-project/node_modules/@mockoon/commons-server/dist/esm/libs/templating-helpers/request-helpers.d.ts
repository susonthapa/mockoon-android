import { Environment } from '@mockoon/commons';
import { Request } from 'express';
import { SafeString } from 'handlebars';
export declare const RequestHelpers: (request: Request, environment: Environment) => {
    body: (path: string, defaultValue: string, stringify: boolean) => SafeString;
    bodyRaw: (...args: any[]) => any;
    urlParam: (paramName: string) => string;
    queryParam: (path: string, defaultValue: string, stringify: boolean) => SafeString;
    queryParamRaw: (...args: any[]) => any;
    header: (headerName: string, defaultValue: string) => string;
    cookie: (key: string, defaultValue: string) => any;
    baseUrl: () => string;
    hostname: () => string;
    ip: () => string;
    method: () => string;
};
