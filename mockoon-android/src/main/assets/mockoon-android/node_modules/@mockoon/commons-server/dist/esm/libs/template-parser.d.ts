import { Environment } from '@mockoon/commons';
import { Request } from 'express';
/**
 * Parse a content with Handlebars
 *
 * @param content
 * @param request
 */
export declare const TemplateParser: (content: string, request: Request, environment: Environment) => string;
